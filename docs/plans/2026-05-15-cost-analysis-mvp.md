# 成本分析软件第一版需求规格与开发计划

**目标：** 做一个 Win7 可用、轻量化、本地运行的成本分析软件，用于从报价单中提取内容，经过人工确认后生成客户可见的成本分析表。

**核心定位：** 软件内部可以保留导入追溯、模板识别、AI 建议、修改记录等辅助信息；导出给客户的成本分析表必须干净，只包含客户需要看到的固定字段。

**技术方向：** C# WinForms + SQLite + 轻量 Excel 读取组件，AI API 作为可选辅助模块预留。

---

## 1. 运行环境

- 操作系统优先支持 Windows 7。
- 软件应本地运行，不依赖浏览器内核。
- 软件应尽量轻量，避免 Electron、WebView2、大型前端框架。
- 数据保存在本地 SQLite 数据库。
- Excel 读取优先使用轻量库，必要时兼容 Excel COM。
- AI 功能通过 API 调用，不做本地大模型计算。
- AI 功能默认关闭，由用户手动配置和调用。

---

## 2. 设计风格

参考 `D:\1\cbjs\Apple网页设计特点.md`，但不照搬网页大屏风格。

采用：

- 白色 / 浅灰主背景。
- 单一蓝色作为主要操作色。
- 简洁表格、清晰层级、少装饰。
- 少阴影、少动画、少复杂视觉效果。
- 内容优先，界面退后。

建议基础视觉：

- 主背景：`#f5f5f7`
- 内容区：`#ffffff`
- 主文字：`#1d1d1f`
- 次级文字：`#6e6e73`
- 主操作蓝：`#0066cc`
- 边框线：`#d2d2d7`
- 字体：微软雅黑、Segoe UI、Arial

---

## 3. 客户成本分析表输出字段

最终导出的客户成本分析表只包含以下字段：

1. No
2. 物料编码
3. 物料名称
4. 物料描述
5. 供应商
6. 材料名称
7. 材料厂家
8. 材料单价
9. 原材料克重
10. 展开尺寸
11. 材料费
12. 印刷费
13. 后工序费
14. 其他
15. 采购单价
16. 总用量
17. 总价

不允许导出以下内部字段：

- 来源文件
- 来源 Sheet
- 来源行号
- 识别模板
- 识别置信度
- AI 建议标记
- 内部备注
- 修改记录

---

## 4. 第一版 MVP 功能范围

第一版必须完成：

- 新建成本分析单。
- 手动新增、编辑、删除成本分析明细。
- 导入 Excel 报价单。
- 支持 `.xls` 和 `.xlsx`。
- 支持最常见普通报价单模板：
  - `序号 / 产品型号 / 成品尺寸(MM) / 材质/工艺 / 数量`
  - `序号 / 料号及名称 / 成品尺寸(MM) / 材质/工艺 / 数量`
- 自动识别供应商、报价日期、报价单号。
- 自动识别表头行、数量阶梯行、数据起始行。
- 自动提取物料编码、物料名称、成品尺寸、材质工艺、阶梯价格。
- 支持物料编码和物料名称从混合文本中拆分。
- 输入客户总用量后自动匹配阶梯单价。
- 生成客户成本分析表。
- 校验采购单价和成本合计。
- 校验总价。
- 导出 Excel。
- 预留 AI API 设置和调用结构。
- 预留内部导入追溯字段。

第一版暂不强制完成：

- PDF 自动识别。
- 图片 OCR。
- AI 完整识别流程。
- 纸箱报价模板完整支持。
- 贴纸/板贴模板完整支持。
- 复杂材料费自动计算。
- 多用户权限。
- 云同步。
- Web 版本。

---

## 5. 核心页面

第一版页面保持少而清楚：

1. 成本分析列表
2. 成本分析编辑
3. 报价单导入确认
4. 材料库
5. 系统设置

后续版本再开放：

- 工艺库
- 模板管理
- 历史成本
- AI 识别日志

---

## 6. 报价单导入流程

导入流程：

1. 用户选择 Excel 报价单。
2. 软件扫描所有 Sheet。
3. 软件判断 Sheet 类型。
4. 软件找到表头行。
5. 软件识别数量阶梯行。
6. 软件识别数据起始行。
7. 软件提取物料行。
8. 软件生成待确认数据。
9. 用户确认或修改。
10. 用户填写客户总用量。
11. 软件匹配采购单价。
12. 用户确认加入成本分析表。

普通报价单识别规则：

- 如果同一行出现 `序号`、`产品型号` 或 `料号及名称`、`成品尺寸`、`材质/工艺`、`数量`，判断为普通报价单。
- 表头行下方如果出现多个数字，则判断为数量阶梯行。
- 数量阶梯行下方为数据起始行。
- 数据行从序号为数字的行开始，遇到 `以下空白` 或空行段落时停止。

---

## 7. 字段映射规则

物料编码可能来源：

- 料号
- 产品编码
- 物料编码
- 产品型号
- 料号及名称

物料名称可能来源：

- 名称
- 物料名称
- 产品名称
- 产品型号
- 料号及名称

物料描述可能来源：

- 规格描述
- 成品尺寸
- 材质/工艺
- 材质工艺

采购单价可能来源：

- 报价
- 成品单价
- 单价
- 数量阶梯价格

展开尺寸可能来源：

- 展开尺寸
- 成品尺寸
- 总长 / 总宽

如果 `料号及名称` 或 `产品型号` 同时包含编码和名称，软件应拆分：

示例：

```text
6-01.56.xxxxxx-0.002 ED-80D试剂包装白盒
```

拆分为：

```text
物料编码：6-01.56.xxxxxx-0.002
物料名称：ED-80D试剂包装白盒
```

---

## 8. 阶梯价格匹配规则

如果报价单只有一个单价：

```text
采购单价 = 该单价
```

如果报价单有单点数量阶梯：

```text
100 / 500 / 1000
```

默认选择不超过客户总用量的最大数量档。

示例：

| 客户总用量 | 采用价格档 |
| ---: | ---: |
| 80 | 100 档，并提示低于最低数量 |
| 100 | 100 档 |
| 300 | 100 档 |
| 500 | 500 档 |
| 800 | 500 档 |
| 1000 | 1000 档 |

如果报价单有区间数量阶梯：

```text
500-2499 / 2500-4999 / 5000-9999
```

按客户总用量命中对应区间。

---

## 9. 成本分析生成规则

每个物料生成一行客户成本分析明细。

基础规则：

```text
总价 = 采购单价 * 总用量
```

成本合计校验：

```text
成本合计 = 材料费 + 印刷费 + 后工序费 + 其他
```

如果 `成本合计` 与 `采购单价` 不一致，软件标记为计算异常，但允许用户确认保留。

如果报价单有成本分析 Sheet：

- 原材料成本优先映射为材料费。
- 加工成本优先映射为后工序费，具体拆分后续版本优化。
- 不良、营销、运费成本优先映射为其他。
- 报价优先映射为采购单价。

如果报价单只有最终报价：

- 采购单价取报价单价。
- 材料费、印刷费、后工序费、其他等待人工补充或后续由材料库、工艺库、AI 建议补全。

---

## 10. 数据库表

第一版建议表：

### suppliers

供应商表：

- id
- 供应商名称
- 简称
- 地址
- 电话
- 传真
- 联系人
- 备注

### materials

材料库：

- id
- 材料名称
- 别名
- 材料类别
- 材料厂家
- 克重规格
- 计价单位
- 含税单价
- 是否含运
- 生效日期
- 失效日期
- 备注

### quote_templates

模板规则表：

- id
- 模板名称
- 模板类型
- 适用供应商id
- 表头关键词
- 表头行规则
- 数量阶梯规则
- 数据起始规则
- 字段映射JSON
- 启用状态
- 使用次数
- 最后使用时间
- 备注

### cost_analysis

成本分析单：

- id
- 分析单号
- 客户名称
- 项目名称
- 供应商id
- 分析日期
- 含税说明
- 含运说明
- 状态
- 备注
- 创建时间
- 更新时间

### cost_analysis_items

成本分析明细：

- id
- 成本分析单id
- No
- 物料编码
- 物料名称
- 物料描述
- 供应商
- 材料名称
- 材料厂家
- 材料单价
- 原材料克重
- 展开尺寸
- 材料费
- 印刷费
- 后工序费
- 其他
- 采购单价
- 总用量
- 总价

### cost_analysis_item_meta

内部追溯字段，不导出：

- id
- 明细id
- 来源导入id
- 来源Sheet
- 来源行号
- 原始文本
- 识别置信度
- 是否人工修改
- 修改记录JSON

### ai_settings

AI 设置：

- id
- 是否启用
- API地址
- API Key 加密值
- 模型名称
- 超时时间
- 每次调用前确认
- 是否允许发送客户名称
- 是否允许发送供应商名称
- 是否允许发送价格

---

## 11. AI 辅助识别预留

AI 是第二识别方案，不替代本地规则。

主力 AI 供应商暂定为 DeepSeek。

DeepSeek 默认配置：

```text
API 地址：https://api.deepseek.com
默认模型：deepseek-v4-flash
高精度模型：deepseek-v4-pro
```

说明：

- DeepSeek API 使用与 OpenAI 风格相近的 Chat Completions 接口。
- JSON 输出场景应使用 `response_format: {"type":"json_object"}`。
- 系统提示词和用户提示词中必须明确要求只输出 JSON。
- 报价单识别第一阶段默认使用 `deepseek-v4-flash`，实际测试中响应更快；`deepseek-v4-pro` 保留为可选高精度模型。
- 请求应设置足够的 `max_tokens`，避免 JSON 被截断；第一版建议 `max_tokens=2000`。
- 旧模型名 `deepseek-chat`、`deepseek-reasoner` 不作为新开发默认值。
- API Key 存储时应做本地加密，不直接明文保存。
- Win7 直连 API 如遇 TLS 或网络兼容问题，后续可增加中转服务，但第一版不强制。

调用时机：

- 本地识别失败。
- 本地识别置信度低。
- 用户手动点击 AI 辅助识别。
- 成本字段缺失，需要建议补全。

AI 只返回结构化建议，不直接写入最终客户表。

AI 输出必须是 JSON。

AI 不允许决定：

- 最终采购单价
- 最终总用量
- 最终总价
- 是否导出客户表

所有金额和总价必须由本地程序重新计算。

---

## 12. AI 系统提示词

### 报价单识别系统提示词

```text
你是一个报价单结构化识别助手，专门处理包装、印刷、纸箱、贴纸、礼盒、彩盒、说明书等物料报价单。

你的任务是：
1. 识别报价单类型。
2. 判断表头行、数量阶梯行、数据起始行。
3. 将报价单中的字段映射到标准成本分析字段。
4. 提取物料编码、物料名称、规格描述、尺寸、材质、工艺、数量阶梯、单价等信息。
5. 从混合文本中拆分物料编码和物料名称。
6. 从“材质/工艺/规格描述”中提取材料名称、克重、尺寸、工艺关键词。
7. 对不确定字段标记为 requires_review，不要猜测成确定结果。

你不能做的事：
1. 不要替用户最终决定采购单价。
2. 不要替用户最终决定总用量。
3. 不要替用户最终决定总价。
4. 不要把推测值当作确定值。
5. 不要输出自然语言解释。
6. 不要输出 Markdown。
7. 不要输出 JSON 以外的任何内容。
8. 不要编造报价单中不存在的数据。
9. 不要自行修改供应商、客户、物料编码、价格。
10. 不要生成最终客户成本分析表，只生成结构化识别建议。

输出必须是严格 JSON，并且必须能被标准 JSON 解析器直接解析。
```

### 成本字段补全系统提示词

```text
你是一个成本字段补全助手，专门根据包装印刷报价单、材料库、工艺库、历史成本记录，为成本分析表提供补全建议。

你的任务是：
1. 根据材质/工艺文本识别材料名称、克重、工艺类别。
2. 根据材料库候选项匹配材料厂家和材料单价。
3. 根据历史成本记录建议材料费、印刷费、后工序费、其他费用。
4. 对每个建议给出 confidence 和 reason。
5. 不确定时必须标记 requires_review。

你不能做的事：
1. 不要替用户最终确认成本字段。
2. 不要替用户最终确认采购单价。
3. 不要替用户计算最终总价。
4. 不要编造材料库或历史记录中没有的价格。
5. 不要输出 JSON 以外内容。
```

---

## 13. 验收标准

第一版完成后，应满足：

- 能在 Win7 环境启动。
- 能创建成本分析单。
- 能手动录入一行成本分析明细。
- 能导入常见普通报价单 Excel。
- 能识别表头行和数量阶梯。
- 能提取至少一条物料数据。
- 能让用户输入总用量并匹配采购单价。
- 能生成客户成本分析表字段。
- 能正确计算总价。
- 能标记成本合计异常。
- 能导出 Excel。
- 导出的 Excel 不包含内部追溯字段。
- 软件无网络时仍可完成本地导入、编辑和导出。

---

## 14. 开发顺序建议

1. 建立 WinForms 项目骨架。
2. 建立 SQLite 数据库初始化。
3. 实现成本分析列表。
4. 实现成本分析编辑页。
5. 实现手动新增明细。
6. 实现 Excel 文件选择。
7. 实现普通报价单扫描和表头识别。
8. 实现字段映射和阶梯价格提取。
9. 实现导入确认页。
10. 实现总用量输入和单价匹配。
11. 实现校验规则。
12. 实现 Excel 导出。
13. 加入基础视觉样式。
14. 预留 AI 设置页和接口结构。
15. 接入 DeepSeek AI 辅助识别：导入确认页可手动调用 AI，AI 返回结构化 JSON 后回填预览表，并用状态列提示是否需要人工复核。
16. 增强 AI 人工确认：AI 修改过的字段使用淡蓝色高亮，单元格悬浮提示显示“原值 -> 新值”，AI 状态列显示改动数量和字段名称。
17. 增加 AI 审计与撤销：导入确认页可查看上一次 AI 原始 JSON 返回，也可撤销上一次 AI 字段改动并恢复回填前的预览摘要。
18. 增加报价单批量预扫描：可选择报价单文件夹，逐个扫描 .xls/.xlsx 文件，显示可导入/未识别、模板、供应商、Sheet、物料数和错误信息；选中可导入文件后进入原有导入确认页。
19. 优化主表行选择 UX：参考 Ant Design Table rowSelection，勾选行整行浅蓝高亮，表头选择列支持全选/取消全选，状态栏显示已选择行数，关闭表格底部自动新增行，新增只能通过“新增明细”按钮。
20. 增加 `.xlsx` 轻量读取路径：新版报价单优先用 OpenXML/ZIP 直接读取单元格内容，不依赖 Excel COM；`.xls` 老格式继续走 Excel COM，保证历史报价单兼容。
21. 增加导入确认页原始文件预览：在导入确认页上方固定展示原报价单 Sheet 内容，下方展示识别结果，中间分隔条可拖动；原始预览高亮表头行、数量行和识别到的物料行，方便人工核对。
22. 增加导入确认页人工修正：识别结果中的物料编码、物料名称、成品尺寸、材质/工艺、材料名称、克重、用量可在确认加入前直接编辑；确认时写回导入物料，用量必须为数字或留空，阶梯价格和 AI 状态保持只读。
23. 增加阶梯价格结构化编辑：导入确认页可对选中物料打开“编辑阶梯价格”窗口，维护阶梯标签、最小数量、最大数量、单价；单价必须为数字，最大数量不能小于最小数量，保存后写回 `PriceTier` 列表并继续参与总用量匹配。
24. 增加主成本分析表阶梯价编辑：主表工具栏和右键菜单提供“编辑阶梯价”，可对当前明细行新增或修改阶梯价格，保存后立即按总用量重新匹配采购单价和总价。
25. 持久化阶梯价格：`cost_analysis_items` 增加 `price_tiers_json` 字段并自动迁移旧库；保存成本分析单时写入每行阶梯价格 JSON，打开历史单据时恢复到行数据并继续用于采购单价匹配。
26. 完善左侧成本分析列表：侧边栏显示最近保存的成本分析单，支持刷新列表，双击单据可直接打开；保存完成后自动刷新最近单据列表。
27. 启动暂缓事项“完整工艺库自动计价”的第一阶段：新增本地工艺费用规则库，可按关键词匹配物料描述并自动填充材料费、印刷费、后工序费或其他费用；主表支持手动应用规则，导入报价单后也会自动尝试填充空白费用，且不覆盖已有人工金额。

---

## 15. 暂缓事项

以下内容不进入第一版核心开发：

- PDF 自动解析。
- 图片报价单 OCR。
- 复杂纸箱核价公式。
- 完整工艺库自动计价。
- 完整 AI 自动补全成本金额。
- 多人协作。
- 云端同步。
- 权限管理。
- 审批流程。
- 大规模报表统计。
